services:
  app:
    build:
      context: .
      target: development
    ports:
      - "8080:8080"
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      NODE_ENV: development
      PORT: 8080
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
